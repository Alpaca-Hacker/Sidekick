<Window x:Class="Sidekick.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sidekick"
        mc:Ignorable="d"
        Title="Dev Toolbox Shell" Height="300" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True"
        Loaded="Window_Loaded" Closing="Window_Closing"
        Name="ShellWindow"
        Opacity="0" IsHitTestVisible="False" >
    <!-- Start hidden and non-interactive -->

    <Window.Resources>
        <!-- Animation Storyboards -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetName="ShellWindow"
                             Storyboard.TargetProperty="Opacity"
                             To="1" Duration="0:0:0.3"/>
            <!-- Optional: Add slide animation later if desired -->
        </Storyboard>
        <Storyboard x:Key="FadeOutAnimation">
            <DoubleAnimation Storyboard.TargetName="ShellWindow"
                             Storyboard.TargetProperty="Opacity"
                             To="0" Duration="0:0:0.3"/>
             <!-- Optional: Add slide animation later if desired -->
        </Storyboard>
    </Window.Resources>

    <!-- Simple visible content area for testing -->
    <Border Background="#FFEEEEF2" CornerRadius="8" BorderBrush="#FFABADB3" BorderThickness="1" Margin="5">
        <Grid>
            <!-- Placeholder for Navigation and Content Area -->
            <TextBlock Text="Toolbox Shell - Ready!"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#FF333333"/>

            <!-- Example Structure for later:
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Background="LightGray" Width="80">
                <!- Navigation Buttons Here ->
            </StackPanel>

            <ContentControl Grid.Column="1" Name="ToolContentArea" Background="White">
                <! - Tool UserControl goes here ->
            </ContentControl>
            -->
        </Grid>
    </Border>
</Window>